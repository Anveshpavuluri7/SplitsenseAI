# Copy to .streamlit/secrets.toml for local dev, or paste contents into
# Streamlit Cloud → App → Settings → Secrets

# When deployed on Streamlit Cloud, set this to your Render backend URL:
API_BASE_URL = "https://splitsenseai-api.onrender.com"

# For local development leave it as the default below (no secrets.toml needed,
# the dashboard falls back to http://localhost:8000 automatically).
